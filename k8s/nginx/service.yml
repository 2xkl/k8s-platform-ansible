---
apiVersion: v1
kind: Service
metadata:
  name: nginx-secret-app
  namespace: demo-app
  labels:
    app: nginx-secret-app
spec:
  type: NodePort
  selector:
    app: nginx-secret-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
